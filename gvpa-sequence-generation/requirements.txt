# Recommended environment: Python 3.10.20+
# 环境：RTX 5060 + WSL
# install the verified CUDA 13.0 build separately with:
# python -m pip install torch==2.11.0 torchvision --index-url https://download.pytorch.org/whl/cu130

requests>=2.31,<3
numpy>=1.26,<3
pandas>=2.2,<3
matplotlib>=3.8,<4