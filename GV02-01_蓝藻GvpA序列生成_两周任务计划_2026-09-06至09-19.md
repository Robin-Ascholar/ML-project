# GV02-01 蓝藻 GvpA 序列生成

## 完整任务计划（5 人小组）

> **课程**：《机器学习综合实践》  
> **项目编号**：GV02-01  
> **项目类型**：蛋白质序列生成 / 生成式机器学习  
> **团队规模**：5 人  
> **两周冲刺周期**：2026-09-06 ～ 2026-09-19  
> **两周硬目标**：至少完成“核心任务 1～4”——数据集构建、EDA、生成 Baseline、两种深度生成模型。为保证这些任务可验收，统一评价流水线的最小版本必须同步跑通。  
> **推荐核心模型**：k-mer Baseline + LSTM 自回归模型 + VAE  
> **当前硬件基线**：RTX 4060 8GB，可满足主线实验。

---

# 0. 先统一全组对项目的理解

## 0.1 一句话版本

给定一批**真实的蓝藻 GvpA 蛋白质序列**，训练若干生成模型，让模型生成新的 GvpA 候选序列，并用统一的计算指标回答：

1. 哪种生成方法生成的序列最像合理的 GvpA？
2. 哪种方法在**合法性 / 新颖性 / 多样性**之间平衡得最好？
3. 模型是真的学到了 GvpA 的序列规律，还是只记住或机械模仿了训练数据？

## 0.2 用计算机学生的语言描述

把蛋白质看成一种特殊字符串：

```text
普通文本：  今 天 天 气 很 好
蛋白质：    M  A  V  E  K  T ...
             ↑
           token
```

天然蛋白质常用约 20 种氨基酸，因此本任务本质上是：

```text
真实 GvpA 字符串集合
        ↓
学习序列分布
        ↓
生成新的字符串
        ↓
Validator / Evaluator
        ↓
判断“像不像真的、是不是新的、是不是多样”
```

## 0.3 项目边界

本项目是**计算生成 + 计算评价**，不是湿实验。

可以说：

- “生成序列在计算指标上表现出 GvpA-like 特征”；
- “候选序列值得进一步实验验证”；
- “某模型在约束满足度、新颖性和多样性之间取得更好平衡”。

不能仅凭本项目说：

- “生成的蛋白一定能正常表达”；
- “一定能组装成真实气囊”；
- “一定具有真实生物功能”。

---

# 1. 项目背景与术语解释

> 原则：组里任何人第一次说出下面的生物学名词时，都应该能用右栏的计算机类比解释。

| 术语 | 最低限度解释 | 计算机类比 |
|---|---|---|
| **GV / Gas Vesicle / 生物气囊** | 某些微生物产生的、由蛋白质构成的微小含气空腔 | 最终由很多蛋白“组件”组装出的结构 |
| **蓝藻 / Cyanobacteria** | 一类能进行光合作用的细菌；本项目只取它们来源的 GvpA | 数据查询的 `organism filter` |
| **Gvp 蛋白** | 与气囊形成、加固、调控等相关的一组蛋白 | 一个功能模块下的多个类 |
| **GvpA** | 气囊主壳体蛋白，是本项目唯一重点生成对象 | 我们要学习和生成的“目标 sequence class” |
| **氨基酸 / Amino Acid** | 构成蛋白质的基本单元，常用 20 个字母表示 | token |
| **蛋白质序列** | 一串氨基酸字符，如 `MAV...` | token sequence / string |
| **FASTA** | 保存蛋白质序列的常见纯文本格式 | 简单的序列数据文件格式 |
| **保守位点** | 很多真实 GvpA 在某个对齐位置都倾向使用相同氨基酸 | 低熵 token position |
| **保守区域 / 结构域** | 一段在进化中较稳定、通常和结构或功能有关的序列模式 | 一段高重要性的 pattern / signature |
| **MSA / 多序列比对** | 把多条相似序列对齐，使相应位置落在同一列 | 多字符串 alignment |
| **Pfam** | 蛋白质家族/结构域 profile 数据库 | pattern / family profile database |
| **HMMER** | 用 profile HMM 判断一条序列与某蛋白家族模式是否匹配 | family pattern matcher |
| **CD-HIT** | 按序列相似度聚类、去掉高度重复序列 | near-duplicate clustering / dedup |
| **BLAST** | 搜索与某条生物序列最相似的已知序列 | biological nearest-neighbor search |
| **Sequence Identity** | 两条对齐序列中相同位置的比例 | 字符串相似度 |
| **PLM** | Protein Language Model，蛋白质语言模型 | 蛋白质领域的 BERT 类模型 |
| **ESM-2** | 常用 PLM，可把序列映射成 embedding | `sequence -> embedding vector` |
| **t-SNE / UMAP** | 高维向量降到二维用于可视化 | 常规表示空间可视化 |
| **MMD** | 衡量两组样本分布差异的指标 | distribution distance |
| **Novelty / 新颖性** | 生成结果不能只是复制训练集 | `1 - nearest_train_similarity` 的思想 |
| **Diversity / 多样性** | 同一模型生成的一批结果不能彼此都一样 | batch 内 pairwise distance / unique ratio |
| **自回归模型** | 根据前面的 token 预测下一个 token | GPT 式 next-token generation |
| **VAE** | 把序列编码成连续 latent vector，再解码/采样生成 | latent-variable generative model |
| **GAN** | 生成器和判别器对抗训练 | 生成模型扩展项；离散序列上较难训练 |

## 1.1 一个必须提前处理的术语风险：Pfam 编号

课程材料当前写的是 **Pfam01132 / PF01132**。此前可行性核验中发现该 accession 存在疑点，因此：

- **M1 阶段必须核对当前 Pfam / InterPro / UniProt 中 GvpA 对应的 family/domain profile；**
- 在老师确认前，代码中不要把 `PF01132` 写死；
- 建议统一使用配置项，例如：

```yaml
gvpa_profile_id: "TO_BE_CONFIRMED"
```

- 如果课程文档与数据库版本不一致，在报告中明确记录：课程原文、数据库版本、最终采用的 profile 及理由。

这不是主任务的障碍，但必须在正式计算“Pfam 通过率”前解决。

---

# 2. 三个研究问题（RQ）

## RQ1：不同生成策略，谁更能生成“像 GvpA”的序列？

至少比较：

- 随机 / k-mer Baseline；
- LSTM 自回归模型；
- VAE；
- GAN 或 Transformer 可作为扩展。

核心不是只看训练 loss，而是比较统一的生成质量指标。

## RQ2：合法性、新颖性、多样性是否互相冲突？

典型可能情况：

- 太像真实数据 → 合法性高，但新颖性低；
- 过度追求新颖 → 可能偏离 GvpA；
- 多样性很高 → 也可能只是生成很多噪声。

目标是找出不同模型的 trade-off，而不是强行给出一个“唯一总分冠军”。

## RQ3：模型是否学到了真实 GvpA 的整体分布？

使用 ESM-2 把真实序列和生成序列转成 embedding，然后通过：

- t-SNE / UMAP 可视化；
- MMD 或核距离辅助定量；
- 离群点分析；

判断生成结果是接近真实 GvpA 分布，还是只学会了表面字符统计。

---

# 3. 总体技术路线

```text
┌──────────────────────────────┐
│  1. 明确 RQ、指标、项目边界 │
└──────────────┬───────────────┘
               ↓
┌──────────────────────────────┐
│  2. 收集蓝藻 GvpA 原始序列 │
│  NCBI / UniProt / 课程资源   │
└──────────────┬───────────────┘
               ↓
┌──────────────────────────────┐
│  3. 清洗 + 去重 + 元数据整理│
│  FASTA / CD-HIT / metadata   │
└──────────────┬───────────────┘
               ↓
┌──────────────────────────────┐
│  4. 数据划分                 │
│  train / valid / test        │
│  防止高相似序列跨集合泄漏   │
└──────────────┬───────────────┘
               ↓
┌──────────────────────────────┐
│  5. EDA                      │
│  长度 / AA组成 / MSA / 保守性│
│  相似度 / family profile覆盖 │
└──────────────┬───────────────┘
               ↓
       ┌───────┴────────┐
       ↓                ↓
┌────────────┐    ┌────────────┐
│ Baseline   │    │ Deep Models│
│ random/kmer│    │ LSTM + VAE │
└─────┬──────┘    └──────┬─────┘
      └──────────┬────────┘
                 ↓
┌──────────────────────────────┐
│  6. 统一生成 N 条候选序列   │
│  输出统一 FASTA + config     │
└──────────────┬───────────────┘
               ↓
┌──────────────────────────────┐
│  7. Unified Evaluation       │
│  合法性 / 新颖性 / 多样性   │
│  长度 / AA组成 / ESM2分布   │
└──────────────┬───────────────┘
               ↓
┌──────────────────────────────┐
│  8. 四项必做实验 + 选做实验 │
└──────────────┬───────────────┘
               ↓
┌──────────────────────────────┐
│  9. 回答 RQ1-RQ3 + M5交付   │
└──────────────────────────────┘
```

---

# 4. 核心任务 1：数据处理与数据集构建

## 4.1 数据来源

优先级：

1. 课程提供的 GV 资源；
2. NCBI；
3. UniProt；
4. 其他公开数据库作为补充。

每条记录至少保留：

```text
sequence_id
sequence
organism
source_database
source_accession
length
raw_annotation
clean_status
remove_reason
```

## 4.2 清洗流程

```text
原始 FASTA
  ↓
格式标准化
  ↓
去完全重复
  ↓
非法字符检查
  ↓
异常长度检查
  ↓
未知字符 X 检查
  ↓
确认蓝藻来源 + GvpA 注释
  ↓
CD-HIT 去冗余
  ↓
family/profile 质量检查
  ↓
最终数据集
```

必须保留：

- `raw` 原始数据，绝不覆盖；
- `cleaned` 规则清洗结果；
- `deduplicated` 去冗余结果；
- 被删除样本的原因日志。

## 4.3 CD-HIT 去冗余

建议至少统计：

```text
原始条数
100% 去重后条数
95% identity 聚类后条数
90% identity 聚类后条数
```

最终阈值不应凭感觉选择，要结合：

- 数据总量；
- 聚类后剩余样本数；
- 模型训练需求；
- 数据泄漏风险。

## 4.4 数据划分

初始推荐：

```text
Train 70%
Valid 15%
Test  15%
```

但**不能只做普通随机划分**。更重要的是：

> 高度相似的序列不要分别落入 Train 和 Test。

建议方法：

1. 先按 CD-HIT / 相似度簇分组；
2. 再以“簇”为单位划分 train / valid / test；
3. 固定 random seed；
4. 保存 `split_info.csv`。

## 4.5 数据任务验收

- [ ] 原始 FASTA 存在；
- [ ] 清洗后 FASTA 存在；
- [ ] 去冗余 FASTA 存在；
- [ ] metadata.csv 完整；
- [ ] 删除日志存在；
- [ ] CD-HIT 阈值与前后样本数有记录；
- [ ] train / valid / test 已生成；
- [ ] 高相似样本不存在明显跨集合泄漏；
- [ ] 全流程可由脚本重跑。

---

# 5. 核心任务 2：探索性数据分析（EDA）

EDA 的目的不是“画几张图”，而是给后续模型设计提供依据。

## 5.1 必做统计

### A. 数据规模

- 原始序列数；
- 清洗后序列数；
- 去冗余后序列数；
- 蓝藻物种数；
- 每个来源数据库数量；
- train / valid / test 数量。

### B. 长度分布

至少输出：

- min / max / mean / median / std；
- histogram；
- boxplot。

用于决定：

- `max_seq_len`；
- padding 策略；
- 生成长度策略。

### C. 氨基酸组成

统计 20 种氨基酸频率，输出：

- 总体频率柱状图；
- train vs test 频率对比；
- 后续 real vs generated 对比基线。

### D. 保守位点

流程：

```text
真实序列
  ↓
MSA 多序列比对
  ↓
每一列统计氨基酸分布
  ↓
计算 entropy / conservation
  ↓
得到保守位点图
```

### E. GvpA family/profile 覆盖

对真实数据先跑 validator，记录：

- profile 数据库版本；
- 阈值；
- 通过数 / 未通过数；
- 未通过样本是否可能是数据问题。

### F. 序列相似度

至少输出：

- pairwise / sample-based similarity 分布；
- 去重前后对比；
- 主要高相似簇数量。

## 5.2 EDA 验收

- [ ] 数据规模表；
- [ ] 长度图；
- [ ] 20 AA 频率图；
- [ ] MSA / 保守性图；
- [ ] family/profile 覆盖统计；
- [ ] 相似度分布；
- [ ] 一页“EDA 对模型设计的结论”。

---

# 6. 核心任务 3：生成 Baseline

Baseline 的意义：

> 证明深度模型是否真的学到了比简单字符统计更多的东西。

## 6.1 Baseline A：按氨基酸频率随机采样

思路：

```text
统计真实数据 AA 频率
        ↓
按概率采样 token
        ↓
按真实长度分布决定长度
        ↓
生成 sequence
```

这是最弱 baseline。

## 6.2 Baseline B：k-mer 生成

`k-mer` = 长度为 k 的连续子串，例如 `MAV`。

建议尝试：

```text
k = 2, 3, 4
```

实现可以采用两种方式：

1. 直接按 k-mer 频率拼接；
2. 更推荐用“前 k-1 个字符 -> 下一个字符”的 Markov 式采样，使片段衔接更自然。

## 6.3 Baseline 必须输出

每种 baseline 至少输出：

```text
generated_sequences.fasta
config.yaml
run.log
metrics.json
```

并记录：

- seed；
- k；
- 生成数量；
- 长度策略；
- 简单合法性指标；
- 新颖性；
- 多样性。

---

# 7. 核心任务 4：深度生成模型

两周硬目标：至少有 **2 种深度模型可以完整训练、保存 checkpoint、生成 FASTA，并接入统一评价脚本。**

推荐：

```text
Model 1：LSTM 自回归
Model 2：VAE
```

## 7.1 统一 tokenizer

词表：

```text
20 个标准氨基酸
+ <PAD>
+ <BOS>
+ <EOS>
+ <UNK>
```

必须由全组共享同一份：

```text
configs/tokenizer.json
```

## 7.2 Model 1：LSTM 自回归模型

### 输入输出

```text
输入： <BOS> M A V E ...
目标： M A V E ... <EOS>
```

### 结构建议

```text
Token ID
  ↓
Embedding 64~128
  ↓
1~2 层 LSTM
hidden 128~256
  ↓
Linear
  ↓
Vocabulary logits
```

不需要大模型。

### Loss

```text
Cross Entropy
```

忽略 `<PAD>`。

### 生成

至少支持：

```text
temperature = 0.7 / 1.0 / 1.3
```

可选支持：

- top-k sampling；
- nucleus sampling。

### 两周验收

- [ ] train loss 可正常下降；
- [ ] validation loss 有记录；
- [ ] checkpoint 可加载；
- [ ] 能从 `<BOS>` 生成到 `<EOS>`；
- [ ] 能批量生成至少 1000 条；
- [ ] 输出统一 FASTA；
- [ ] 能被 evaluation pipeline 读取。

## 7.3 Model 2：VAE

### 核心结构

```text
sequence
  ↓
Encoder
  ↓
mu, logvar
  ↓
reparameterization
  ↓
latent z
  ↓
Decoder
  ↓
sequence
```

### Loss

```text
Total Loss = Reconstruction Loss + beta * KL
```

需要记录：

- reconstruction loss；
- KL loss；
- beta；
- latent_dim。

### 推荐起点

```text
latent_dim = 32 / 64
hidden_dim = 128 / 256
beta = 0.1 / 0.5 / 1.0 中选小规模尝试
```

### 两周验收

- [ ] 能重构训练序列；
- [ ] loss 曲线正常；
- [ ] checkpoint 可加载；
- [ ] 能从随机 z 生成；
- [ ] 能生成至少 1000 条；
- [ ] 输出统一 FASTA；
- [ ] 能进入统一评价流水线。

## 7.4 Transformer / GAN 怎么处理

### Transformer

可作为 LSTM 的升级/替代对比，但两周内**不是硬要求**。如果 LSTM 很快完成，可加一个 tiny Transformer：

```text
layers = 2~4
hidden = 128~256
heads = 4~8
```

### GAN / SeqGAN

作为扩展，不建议成为两周主线，因为：

- 离散序列生成训练麻烦；
- 训练不稳定；
- 容易 mode collapse；
- 人力成本大于算力成本。

---

# 8. 统一评价指标

> 所有模型必须使用同一套 evaluator。任何模型负责人不得自行定义另一套“有利于自己模型”的指标。

## 8.1 基础合法性

### 8.1.1 字符合法率

```text
valid_char_rate = 只含标准 AA 的序列数 / 总生成数
```

### 8.1.2 长度合理率

根据真实数据定义合理区间，例如：

- `[P1, P99]`；或
- `[min_real, max_real]`；

最终必须在报告中明确规则。

### 8.1.3 GvpA family/profile 通过率

```text
family_pass_rate =
通过 GvpA profile 检查的生成序列数 / 生成总数
```

同时保存：

- E-value；
- bit score；
- 阈值；
- profile/database 版本。

不要只保存 pass/fail。

## 8.2 新颖性 Novelty

对每条生成序列 `g_i`：

```text
max_similarity_i = max_j similarity(g_i, real_j)
```

可以定义：

```text
novelty_i = 1 - max_similarity_i
```

并同时报告多档阈值：

```text
max similarity < 70%
max similarity < 80%
max similarity < 90%
```

优先采用全局序列 identity；BLAST 作为搜索和辅助分析工具。

## 8.3 多样性 Diversity

至少报告：

1. **Unique Ratio**

```text
unique_sequences / generated_sequences
```

2. **平均 pairwise distance / similarity**

3. **最大簇占比**（可选但很有用）

用于发现 mode collapse。

## 8.4 氨基酸组成差异

比较真实数据与生成数据的 20 AA 频率，可使用：

- MAE；
- Jensen-Shannon divergence；
- 频率相关系数。

## 8.5 ESM-2 嵌入分布

统一：

- 同一个 ESM-2 模型；
- 同一种 pooling；
- 同一个 random seed；
- 同样的降维参数。

输出：

- t-SNE 或 UMAP 图；
- MMD / 核距离；
- 离群点列表。

注意：

> t-SNE 是 visualization，不是证明“生物有效”的证据。

## 8.6 不推荐“直接相加得到总分”

主要结果应该分别报告：

```text
合法性
新颖性
多样性
分布距离
```

推荐：

- trade-off scatter；
- Pareto front；
- 雷达图仅用于展示，不作为严格排名依据。

---

# 9. 四项必做实验

## 实验 1：生成策略对比（RQ1）

### 对比对象

至少：

```text
Random / k-mer
LSTM
VAE
```

### 控制变量

- 同一训练集；
- 同一 test / real reference；
- 每种方法生成相同数量，例如 1000 条；
- 相同长度策略；
- 相同 evaluator；
- 相同阈值；
- 最好重复 3 次，报告 mean ± std。

### 输出主表

| Model | Family Pass ↑ | Novelty ↑ | Unique Ratio ↑ | Pairwise Diversity ↑ | AA Dist ↓ | ESM2 MMD ↓ |
|---|---:|---:|---:|---:|---:|---:|
| Random |  |  |  |  |  |  |
| k-mer |  |  |  |  |  |  |
| LSTM |  |  |  |  |  |  |
| VAE |  |  |  |  |  |  |

### 必须回答

- 谁最“像 GvpA”？
- 谁最有新颖性？
- 谁最多样？
- 谁最均衡？

---

## 实验 2：三目标权衡分析（RQ2）

目标：研究合法性 / 新颖性 / 多样性的 trade-off。

### LSTM 可通过

```text
temperature = 0.6 / 0.8 / 1.0 / 1.2 / 1.4
```

### VAE 可通过

- 改变 latent sampling scale；
- 改变 beta；
- 在真实 latent 附近扰动。

### 输出

- 合法性 vs 新颖性；
- 合法性 vs 多样性；
- 新颖性 vs 多样性；
- Pareto 前沿；
- 必要时三维散点图。

### 必须回答

- 三个指标是否冲突？
- 是否存在“甜点区域”？
- 不同模型偏向哪个目标？

---

## 实验 3：消融实验

目标：证明每个设计到底带来了什么。

推荐优先做一个**清晰而不贪多**的消融链：

```text
Random
  ↓
k-mer
  ↓
LSTM
  ↓
+ realistic length sampling
  ↓
+ conserved-position constraint（若完成）
```

也可以针对 LSTM：

```text
Basic LSTM
vs
+ temperature sampling
vs
+ length constraint
vs
+ post-filter
```

或针对 VAE：

```text
beta = 0.1 / 0.5 / 1.0
```

### 必须回答

- 哪一步提高合法性？
- 哪一步影响新颖性？
- 哪一步降低多样性？
- 更复杂是否真的更好？

---

## 实验 4：ESM-2 嵌入空间分析（RQ3）

### 对象

```text
真实 Train
真实 Test
k-mer
LSTM
VAE
```

### 流程

```text
sequence
  ↓
ESM-2
  ↓
embedding
  ↓
统一 pooling
  ↓
UMAP / t-SNE
  ↓
MMD / 核距离
```

### 必须分析

- 生成样本是否靠近真实分布？
- 是否全部挤成一个点（mode collapse）？
- 是否大量远离真实区域（可能是幻觉样本）？
- 是否只是复制真实样本？

---

# 10. 选做与探索实验

## 选做 1：约束生成（优先级最高）

### 硬约束

- 长度限制；
- 固定 / 限制部分保守位置；
- 禁止非法字符；
- generation 后 family/profile 过滤。

### 软约束

- 对偏离真实 AA composition 的样本惩罚；
- 对过度相似训练集的结果惩罚；
- 对违反 family/profile 特征的结果惩罚（实现难度较高）。

### 价值

直接回答：

> 加强“合法性”是否会牺牲 novelty / diversity？

## 选做 2：条件生成

例如使用物种作为 condition。

只在数据量和 metadata 足够时做。

## 选做 3：生成序列功能预测

如果课程其他小组 / 任务已有稳定的 GvpA predictor，可以把生成结果送入 predictor 做辅助评价。

注意 predictor 对 OOD 生成序列可能不可靠，只能辅助。

## 探索项：AlphaFold / ESMFold

仅用于少量候选的结构层面探索，不建议两周内作为主目标。

---

# 11. M1-M5 里程碑

## M1：问题分析

### 必做

- [ ] 项目背景与边界；
- [ ] 所有关键术语解释；
- [ ] RQ1-RQ3；
- [ ] 数据来源；
- [ ] 数据清洗 / 去重 / split 方案；
- [ ] 指标定义；
- [ ] 模型路线；
- [ ] 实验矩阵；
- [ ] 五人分工；
- [ ] Git 仓库与协作规范；
- [ ] GvpA family/Pfam profile 编号核验。

### 建议交付

```text
docs/M1_problem_definition.md
configs/project.yaml
results/plans/experiment_matrix.csv
```

---

## M2：初步结果

### 必做

- [ ] 原始数据；
- [ ] 清洗 / 去重；
- [ ] metadata；
- [ ] train / valid / test；
- [ ] 长度分析；
- [ ] AA composition；
- [ ] MSA / conservation；
- [ ] family/profile 覆盖；
- [ ] 相似度分析；
- [ ] EDA 图表与结论。

### 交付

```text
docs/M2_eda.md
data/
results/figures/eda/
results/tables/eda/
```

---

## M3：中期结果

### 必做

- [ ] Random / k-mer baseline；
- [ ] 至少一个简单生成模型；
- [ ] 能批量生成 FASTA；
- [ ] 初步合法性、新颖性、多样性指标；
- [ ] 训练日志；
- [ ] 生成配置；
- [ ] 中期对比表。

---

## M4：完整实验

### 必做

- [ ] ≥2 深度生成模型；
- [ ] 实验 1：策略对比；
- [ ] 实验 2：三目标权衡；
- [ ] 实验 3：消融；
- [ ] 实验 4：ESM-2 分布分析；
- [ ] 异常样本分析；
- [ ] 选做实验最多 2 项；
- [ ] 完整图表。

---

## M5：最终成果

### 必须提交

- [ ] 完整实验报告；
- [ ] 生成序列 FASTA；
- [ ] 可复现 Git 仓库；
- [ ] README；
- [ ] requirements / environment；
- [ ] 模型 checkpoint 或下载说明；
- [ ] 实验表格和图；
- [ ] 答辩 PPT；
- [ ] 对 RQ1-RQ3 的明确回答；
- [ ] 局限性与未来工作。

> M4/M5 的最终日期按课程后续安排执行，本计划不擅自添加课程未给出的截止日期。

---

# 12. 两周冲刺计划：2026-09-06 ～ 2026-09-19

## 12.1 两周硬验收目标

这里将“前四项必做任务”明确为：

1. **Task 1：真实 GvpA 数据收集、清洗、去重、划分；**
2. **Task 2：完成 EDA；**
3. **Task 3：完成 Random / k-mer Baseline；**
4. **Task 4：完成 LSTM + VAE 两种深度生成模型。**

为了让 3、4 真正可验收，必须同步完成 evaluator 的最小版本：

- 字符 / 长度合法性；
- family/profile 检查接口；
- novelty；
- unique ratio / diversity；
- 统一结果 JSON/CSV。

**9 月 19 日不是要求整个 M4/M5 全部结束，而是要求上述四个核心任务全部“可运行、可复现、可验收”。**

---

## 12.2 分阶段安排

### Phase 0：9/6 —— Kickoff 与工程冻结

**全组共同完成：**

- 建 Git 仓库；
- 创建目录结构；
- 明确 P1-P5 分工；
- 确定统一 FASTA / metadata schema；
- 确定 tokenizer；
- 确定模型统一 `generate()` 输出格式；
- 确定 evaluator 输入输出格式；
- 创建 issue / project board；
- 把 Pfam/profile 编号核验设为阻塞项。

**当天验收：**

```text
repo 可以 clone
requirements 可安装
main 分支保护规则明确
README 有“如何开始”
每个人至少一个 issue
```

---

### Phase 1：9/7 ～ 9/9 —— 数据流水线 + M1 完成

#### P1 数据负责人

- 收集课程 / NCBI / UniProt 数据；
- raw FASTA 落盘；
- metadata 整理；
- 清洗规则实现；
- exact dedup + CD-HIT；
- 输出去重前后统计。

#### P2 生物信息 / Validator 负责人

- 确认 GvpA family/profile；
- 跑通 HMMER / profile 检查；
- 对真实序列做 sanity check；
- 准备 MSA 流程。

#### P3 Baseline + LSTM 负责人

- 实现 tokenizer / dataset loader；
- 实现 random/k-mer baseline 骨架；
- 建 LSTM 模型训练骨架。

#### P4 VAE 负责人

- 复用统一 tokenizer；
- 建 VAE encoder/decoder；
- 用小规模假数据跑通 forward / loss。

#### P5 Evaluation + Integration 负责人

- 建统一 evaluator；
- 先支持字符、长度、unique ratio；
- 接 P2 family validator；
- 建 results schema。

**9/9 验收：**

- [ ] M1 文档完成；
- [ ] raw → cleaned → dedup 流程可跑；
- [ ] profile validator 至少在真实样本上跑通；
- [ ] 模型和评价接口冻结 v0.1。

---

### Phase 2：9/10 ～ 9/11 —— EDA + 数据集冻结

#### P1 + P2 主责

完成：

- 数据规模；
- 长度分布；
- AA composition；
- MSA / conservation；
- family/profile 覆盖；
- similarity distribution；
- cluster-aware split。

#### P3

- k-mer `k=2/3/4`；
- baseline 能生成 FASTA。

#### P4

- VAE 在正式 train 子集上开始小规模训练；
- 检查 reconstruction 是否正常。

#### P5

- novelty v0；
- diversity v0；
- 结果表自动汇总脚本。

**9/11 第一次大检查：**

- [ ] **Task 1 完成；**
- [ ] **Task 2 完成；**
- [ ] 数据集版本冻结：`dataset_v1`；
- [ ] M2 报告可以形成；
- [ ] 不再随意修改 train/test。

> 除非发现数据错误，否则 9/11 后任何数据集变更必须升版本，例如 `dataset_v2`，并在实验日志中说明。

---

### Phase 3：9/12 ～ 9/14 —— Baseline + 第一轮深度模型

#### P3

- baseline 全跑；
- LSTM 正式训练；
- 先跑最小配置，再调 hidden/layers；
- 生成第一批 1000 条序列。

#### P4

- VAE 正式训练；
- 记录 reconstruction / KL；
- 尝试 2~3 组 beta / latent_dim；
- 生成第一批 1000 条序列。

#### P2

- 对 baseline / LSTM / VAE 样本批量跑 validator；
- 排查明显异常序列。

#### P5

- 将三类 generated FASTA 统一接入 evaluator；
- 产出首张对比表。

#### P1

- 数据脚本补测试；
- 帮助检查生成结果的长度 / composition 与真实数据差异。

**9/14 验收：**

- [ ] **Task 3 完成；**
- [ ] LSTM 第一版可训练 + 可生成；
- [ ] VAE 第一版可训练 + 可生成；
- [ ] 至少有一张 `Baseline vs LSTM vs VAE` 初步指标表。

---

### Phase 4：9/15 ～ 9/17 —— 两种深度模型收敛与统一评测

重点不是继续堆模型，而是把已有模型做完整。

#### LSTM

至少比较：

```text
temperature = 0.7 / 1.0 / 1.3
```

#### VAE

至少比较两组：

```text
beta / latent sampling setting
```

#### 统一要求

每个最终配置：

- 固定 seed；
- 保存 config；
- 保存 checkpoint；
- 生成统一数量；
- 写入 run ID；
- 跑完最小 evaluator；
- 保存日志。

**9/17 验收：**

- [ ] LSTM 稳定版本；
- [ ] VAE 稳定版本；
- [ ] 每个模型可一条命令训练；
- [ ] 每个模型可一条命令生成；
- [ ] evaluator 能自动读取输出。

---

### Phase 5：9/18 —— 集成、复现、修 Bug

当天不再新增大功能。

执行：

1. 从干净环境 clone 仓库；
2. 按 README 准备数据；
3. 重新跑 baseline；
4. 加载 checkpoint 生成；
5. 运行 evaluation；
6. 确认图表和 CSV 能生成；
7. 检查所有相对路径；
8. 删除硬编码个人目录；
9. 检查随机种子和配置文件；
10. 标记 `v0.1-two-week-sprint`。

---

### Phase 6：9/19 —— 两周最终验收

**硬性必须通过：**

#### Task 1 数据

- [ ] 数据可重建；
- [ ] 清洗 / 去重 / split 完成；
- [ ] 数据泄漏检查完成。

#### Task 2 EDA

- [ ] 核心 EDA 图和结论齐全。

#### Task 3 Baseline

- [ ] Random/k-mer 至少一个主 baseline 完整；
- [ ] 能生成；
- [ ] 能评价。

#### Task 4 两个深度模型

- [ ] LSTM 完整；
- [ ] VAE 完整；
- [ ] 都可保存 / 加载；
- [ ] 都可批量生成；
- [ ] 都接入统一 evaluator。

#### 同步要求

- [ ] 有一张初步模型对比表；
- [ ] README 可让其他组员复现；
- [ ] Git 无关键未提交代码；
- [ ] 所有核心运行都有 config + log；
- [ ] 已列出下一阶段四项必做实验的执行矩阵。

### 9/19 的 Stretch Goal（不是硬要求）

如果进度顺利：

- [ ] 实验 1（生成策略对比）完成第一版；
- [ ] ESM-2 embedding pipeline 跑通；
- [ ] LSTM temperature trade-off 有初步图；
- [ ] 约束生成有 prototype。

---

# 13. 五人分工

> 不建议“五个人一人一个模型”。推荐按系统模块分，使接口清晰、结果可拼接。

## P1：Data & Reproducibility —— 数据工程负责人

### 主责

- 数据下载；
- metadata；
- 清洗；
- CD-HIT；
- 数据划分；
- 数据版本；
- preprocessing 的可复现性。

### 必须交付

```text
data/
preprocessing/
results/tables/dataset_stats.csv
```

### 次责

- 协助 EDA；
- 检查 real vs generated 的长度 / AA composition。

---

## P2：Bioinformatics & Validator —— 生物信息/约束验证负责人

### 主责

- Pfam/profile accession 核验；
- HMMER pipeline；
- MSA；
- conservation；
- family/profile pass；
- 真实数据 sanity check。

### 这个人只需要掌握四件事

```text
MSA = 字符串对齐
Conserved position = 低熵位置
Pfam/Profile = 家族模式数据库
HMMER = 家族匹配器
```

### 必须交付

```text
bioinformatics/
evaluation/family_profile.py
results/figures/conservation/
```

---

## P3：Baseline & Autoregressive —— Baseline + LSTM 负责人

### 主责

- tokenizer / dataset loader（和全组共同维护）；
- random baseline；
- k-mer baseline；
- LSTM；
- sampling / temperature；
- model checkpoint。

### 必须交付

```text
models/baselines/
models/lstm/
experiments/train_lstm.py
experiments/generate_lstm.py
```

---

## P4：VAE & Advanced Generation —— 第二生成模型负责人

### 主责

- VAE；
- latent sampling；
- beta / latent_dim 小规模超参对比；
- checkpoint；
- 后期选做约束生成或 GAN。

### 必须交付

```text
models/vae/
experiments/train_vae.py
experiments/generate_vae.py
```

### 后续优先级

```text
约束生成 > Tiny Transformer > GAN
```

除非课程明确要求 GAN。

---

## P5：Evaluation & Integration —— 统一评价与总集成负责人

### 主责

- evaluator framework；
- novelty；
- diversity；
- AA composition distance；
- ESM-2 embedding；
- t-SNE / UMAP；
- MMD；
- 自动汇总表；
- 集成测试。

### 必须交付

```text
evaluation/
analysis/
results/tables/model_comparison.csv
results/figures/model_comparison/
```

### 为什么这个岗位很关键

此岗位能看到：

```text
data -> all models -> all generated outputs -> all metrics -> final conclusions
```

因此也是最适合项目总协调/答辩主讲的人选之一。

---

## 13.1 所有人共同责任

以下工作不能甩给某一个人：

- Git commit；
- README；
- 自己模块的运行说明；
- 自己实验的日志；
- 能解释自己负责指标/模型；
- Code Review；
- 最终报告内容确认。

### 建议协作规则

- `main`：只放稳定版本；
- 每人使用 `feature/...` 分支；
- 每个 PR 至少 1 人 review；
- 大文件、数据和 checkpoint 不直接乱塞 Git；
- 所有实验必须绑定 `config + run_id`。

---

# 14. 推荐代码目录

```text
gvpa-sequence-generation/
├── README.md
├── requirements.txt
├── .gitignore
├── configs/
│   ├── project.yaml
│   ├── tokenizer.json
│   ├── data.yaml
│   ├── baseline.yaml
│   ├── lstm.yaml
│   ├── vae.yaml
│   └── evaluation.yaml
│
├── data/
│   ├── raw/
│   │   ├── course/
│   │   ├── ncbi/
│   │   └── uniprot/
│   ├── cleaned/
│   ├── deduplicated/
│   ├── metadata/
│   └── splits/
│       ├── train.fasta
│       ├── valid.fasta
│       ├── test.fasta
│       └── split_info.csv
│
├── preprocessing/
│   ├── download_data.py
│   ├── parse_fasta.py
│   ├── clean_sequences.py
│   ├── run_cdhit.sh
│   ├── build_metadata.py
│   ├── split_by_cluster.py
│   └── validate_split.py
│
├── bioinformatics/
│   ├── run_msa.sh
│   ├── conservation.py
│   ├── run_hmmer.sh
│   └── profile_config.yaml
│
├── src/
│   ├── tokenization/
│   │   └── amino_acid_tokenizer.py
│   ├── data/
│   │   └── dataset.py
│   ├── models/
│   │   ├── baselines/
│   │   │   ├── random_generator.py
│   │   │   └── kmer_generator.py
│   │   ├── lstm/
│   │   │   └── model.py
│   │   └── vae/
│   │       └── model.py
│   └── utils/
│       ├── seed.py
│       ├── io.py
│       └── logging.py
│
├── evaluation/
│   ├── basic_validity.py
│   ├── family_profile.py
│   ├── novelty.py
│   ├── diversity.py
│   ├── aa_composition.py
│   ├── esm_embedding.py
│   ├── distribution_distance.py
│   └── evaluate_all.py
│
├── experiments/
│   ├── run_baseline.py
│   ├── train_lstm.py
│   ├── generate_lstm.py
│   ├── train_vae.py
│   ├── generate_vae.py
│   ├── exp1_model_comparison.py
│   ├── exp2_tradeoff.py
│   ├── exp3_ablation.py
│   └── exp4_embedding.py
│
├── analysis/
│   ├── eda.py
│   ├── plot_eda.py
│   ├── plot_model_comparison.py
│   └── analyze_outliers.py
│
├── results/
│   ├── generated_sequences/
│   │   ├── baseline/
│   │   ├── lstm/
│   │   └── vae/
│   ├── checkpoints/
│   ├── logs/
│   ├── tables/
│   └── figures/
│       ├── eda/
│       ├── comparison/
│       ├── tradeoff/
│       └── embedding/
│
├── docs/
│   ├── M1_problem_definition.md
│   ├── M2_eda.md
│   ├── experiment_notes.md
│   └── final_report_outline.md
│
└── tests/
    ├── test_preprocessing.py
    ├── test_tokenizer.py
    └── test_evaluation.py
```

---

# 15. 统一接口规范

这是多人协作能否成功的关键。

## 15.1 生成器统一输入输出

推荐所有 generator 都实现类似接口：

```python
sequences = generator.generate(
    num_samples=1000,
    seed=42,
    **generation_config,
)
```

输出统一为：

```text
results/generated_sequences/<model>/<run_id>.fasta
```

## 15.2 evaluator 统一输入输出

```python
metrics = evaluate_all(
    generated_fasta="...",
    reference_fasta="data/splits/train.fasta",
    config="configs/evaluation.yaml",
)
```

输出：

```json
{
  "num_generated": 1000,
  "valid_char_rate": 1.0,
  "length_valid_rate": 0.0,
  "family_pass_rate": 0.0,
  "novelty_lt_80": 0.0,
  "novelty_lt_90": 0.0,
  "unique_ratio": 0.0,
  "mean_pairwise_similarity": 0.0,
  "aa_js_divergence": 0.0,
  "esm2_mmd": null
}
```

## 15.3 Run ID

推荐：

```text
<model>_<date>_<short_config>_<seed>
```

例如：

```text
lstm_20260916_h256_l2_t1.0_s42
```

---

# 16. 4060 8GB 算力计划

主线完全可做，但要避免“大模型冲动”。

## 16.1 可以放心跑

- random / k-mer：CPU；
- LSTM：GPU 余量很大；
- VAE：GPU 余量很大；
- tiny Transformer：可以；
- HMMER / CD-HIT / MSA：主要 CPU；
- ESM-2 小 / 中模型 inference：可以。

## 16.2 建议

- LSTM hidden 不必超过 256 起步；
- Transformer 不必超过 2~4 层起步；
- ESM-2 只做 embedding，不训练；
- ESM-2 分 batch 推理；
- inference 使用 `torch.no_grad()`；
- 显存不够先减 batch size；
- 可使用 FP16 / autocast；
- 不要把 650M+ 模型当硬要求。

## 16.3 单卡五人共享

按任务错峰：

```text
白天 / CPU：数据、CD-HIT、MSA、HMMER、绘图
GPU slot A：LSTM
GPU slot B：VAE
GPU slot C：ESM-2 embedding
```

建议建立一个共享表记录 GPU 时段，避免两个人互相抢卡。

---

# 17. 风险与应对

## Risk 1：去重后数据太少

### 现象

90% identity 去重后只剩很少序列。

### 应对

- 优先小模型；
- 降低模型参数量；
- frozen PLM 只用于评价；
- 重新审视 CD-HIT 阈值；
- 不为了数据量保留明显重复样本；
- 把“小样本下模型复杂度与记忆化”的现象写入分析。

---

## Risk 2：Pfam/profile 编号不一致

### 应对

- 9/7 前核验；
- profile ID 配置化；
- 记录数据库版本；
- 必要时向老师确认；
- 不允许直到实验末期才发现 validator 用错。

---

## Risk 3：LSTM/VAE 生成结果“全一样”

### 可能原因

- 过拟合；
- temperature 太低；
- VAE posterior collapse；
- 数据本身高度保守。

### 应对

- 调 temperature；
- 查看 unique ratio；
- 调 beta；
- 降低模型规模；
- 增加生成随机性；
- 不只看 train loss。

---

## Risk 4：模型生成“很新”，但完全不像 GvpA

### 应对

- family/profile validator；
- length filter；
- AA composition；
- ESM-2 分布；
- 后续约束生成。

---

## Risk 5：五个人代码无法合并

### 应对

第一天冻结：

- tokenizer；
- FASTA schema；
- metadata schema；
- generator interface；
- evaluator interface；
- result schema；
- config 规范。

---

## Risk 6：实验很多，但没有研究结论

每次实验结束必须写一句：

```text
观察到了什么？
为什么可能这样？
它回答了哪个 RQ？
下一步因此做什么？
```

否则不允许只把图扔进 `results/`。

---

# 18. 最终验收清单

## 18.1 数据与可复现性

- [ ] 原始数据来源可追溯；
- [ ] raw 数据未覆盖；
- [ ] 清洗规则代码化；
- [ ] 删除原因有日志；
- [ ] CD-HIT 阈值有记录；
- [ ] 去重前后数据量有记录；
- [ ] train / valid / test 固定；
- [ ] 数据泄漏检查完成；
- [ ] 数据版本可追踪。

## 18.2 EDA

- [ ] 数据规模；
- [ ] 长度分布；
- [ ] AA composition；
- [ ] MSA；
- [ ] conservation；
- [ ] family/profile 覆盖；
- [ ] similarity distribution；
- [ ] EDA 对模型设计的文字结论。

## 18.3 Baseline

- [ ] Random 或 AA-frequency baseline；
- [ ] k-mer baseline；
- [ ] 可批量生成；
- [ ] 输出 FASTA；
- [ ] config / seed / log 完整；
- [ ] 统一评价完成。

## 18.4 LSTM

- [ ] 训练；
- [ ] 验证；
- [ ] checkpoint；
- [ ] generate；
- [ ] temperature；
- [ ] 1000 条批量生成；
- [ ] evaluator 接入。

## 18.5 VAE

- [ ] encoder/decoder；
- [ ] reconstruction loss；
- [ ] KL loss；
- [ ] checkpoint；
- [ ] latent sampling；
- [ ] 1000 条批量生成；
- [ ] evaluator 接入。

## 18.6 评价

- [ ] 字符合法率；
- [ ] 长度合理率；
- [ ] family/profile pass；
- [ ] novelty；
- [ ] unique ratio；
- [ ] pairwise diversity；
- [ ] AA composition distance；
- [ ] ESM-2 embedding；
- [ ] MMD / kernel distance；
- [ ] 异常样本可回溯到原序列。

## 18.7 必做实验

- [ ] 实验 1：策略对比；
- [ ] 实验 2：三目标权衡；
- [ ] 实验 3：消融；
- [ ] 实验 4：嵌入空间分析。

## 18.8 选做实验

最多选 2 项：

- [ ] 约束生成；
- [ ] 条件生成；
- [ ] 功能预测辅助评价；
- [ ] 其他自定义研究问题。

## 18.9 工程

- [ ] README 从零可执行；
- [ ] requirements / environment 完整；
- [ ] 无个人绝对路径；
- [ ] main 分支可运行；
- [ ] 所有实验有 config；
- [ ] 所有实验有 seed；
- [ ] 所有结果有 run_id；
- [ ] 关键脚本有命令示例。

## 18.10 最终报告 / 答辩

- [ ] 清楚解释 GV / GvpA / FASTA / MSA / HMMER / Pfam / CD-HIT / ESM-2；
- [ ] 清楚说明项目只做计算验证；
- [ ] 明确回答 RQ1；
- [ ] 明确回答 RQ2；
- [ ] 明确回答 RQ3；
- [ ] 不只报“最好模型”，而是解释 trade-off；
- [ ] 展示失败案例；
- [ ] 展示局限性；
- [ ] 不把计算合法性夸大成真实生物功能。

---

# 19. 9 月 19 日的最小成功状态

到 2026-09-19，如果以下链路能够完整跑通，这两周就算成功：

```text
真实蓝藻 GvpA 数据
        ↓
清洗 + CD-HIT + split
        ↓
EDA
        ↓
┌─────────┬─────────┬─────────┐
│ k-mer  │  LSTM   │   VAE   │
└────┬────┴────┬────┴────┬────┘
     ↓         ↓         ↓
     generated FASTA（统一格式）
               ↓
       unified evaluator
               ↓
合法性 / 新颖性 / 多样性 / 基础分布指标
               ↓
       初步模型对比表
```

这意味着：

- 数据端已经稳定；
- 两种深度模型已经可用；
- 后面不需要重新搭基础设施；
- M4 的四项实验只是基于已有 pipeline 系统扩展；
- 之后的主要工作从“把系统跑起来”切换为“做严谨实验、解释结果”。

---

# 20. 推荐的项目主线表述

最终整个项目最好始终围绕下面这个逻辑讲：

```text
真实 GvpA 有什么序列规律？
        ↓
不同生成模型能否学到这些规律？
        ↓
生成结果是否仍满足 GvpA 的基本计算约束？
        ↓
它们是否不是简单复制训练集？
        ↓
它们是否保持候选集多样性？
        ↓
真实序列和生成序列在 ESM-2 表示空间中是什么关系？
        ↓
不同模型在“合法性—新颖性—多样性”之间有什么 trade-off？
```

只要最终能够用实验数据把这条链讲完整，这个项目就已经形成了一个完整、清晰、可复现的机器学习生成项目。
