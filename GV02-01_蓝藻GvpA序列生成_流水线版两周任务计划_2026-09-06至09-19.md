# GV02-01 蓝藻 GvpA 序列生成

## 流水线版两周任务计划（5 人小组）

> 课程：《机器学习综合实践》
> 项目编号：GV02-01
> 周期：2026-09-06 ～ 2026-09-19
> 团队规模：5 人
> 硬件基线：RTX 4060 Laptop GPU，8GB 显存
> 文档定位：正文用于全组执行，附录用于查阅技术细节。

---

# 第一部分：全组执行版

## 1. 一页总览

### 当前实际状态（2026-09-07）

数据部分已经完成并冻结为 `gvpa-sequence-generation/data/processed/dataset_v1`：165条非重复蓝藻GvpA，train/valid/test为139/13/13；官方PF00741、本地核心profile、真实MAFFT MSA和95%跨集合泄漏检查全部通过。后续所有模型统一使用冻结目录中的拆分文件，任何数据变更必须另建 `dataset_v2`。

另外已冻结独立辅助预训练集 `gvpa-sequence-generation/data/processed/aux_pan_gvpa_v1`：550条经过蓝藻 valid/test 80% identity + 80% 双向 coverage 隔离的高可信非蓝藻GvpA，默认取302条95%去冗余代表，与139条蓝藻train合并成441条预训练输入。它只用于LSTM/VAE的“预训练→蓝藻微调”对照实验，不得修改或替代固定的蓝藻valid/test。

### 项目目标

使用真实的蓝藻 GvpA 序列训练生成模型，比较不同方法在约束满足度、新颖性和多样性上的表现。

```text
蓝藻 GvpA 数据
→ 清洗、去重、划分
→ AA-frequency / k-mer / LSTM / VAE
→ 统一生成 FASTA
→ 统一评价
→ 模型对比与权衡分析
```

### 两周硬目标

| 模块     | 必须交付                                                                   |
| -------- | -------------------------------------------------------------------------- |
| 数据     | `dataset_v1`、metadata、删除日志、split、MSA、基础 EDA                   |
| Baseline | AA-frequency/random、k-mer、生成 FASTA                                     |
| 深度模型 | LSTM、VAE、checkpoint、config、训练日志、生成 FASTA                        |
| 评价     | 合法性、family、nearest-train identity、novelty、diversity、AA composition |
| 实验     | 模型对比表、一组 temperature/beta 权衡实验                                 |
| 工程     | README、固定 seed、统一命令和输出目录                                      |

### 两周暂不作为硬要求

GAN、Diffusion、ESM-2 微调生成、条件/跨物种生成、大规模结构预测、毒性/组装性综合评分。主线完成后再选择扩展。

### 项目边界

本项目只能说明候选在计算指标上具有 GvpA-like 特征，不能声称其一定能够表达、折叠或组装成真实气囊。

## 2. 三组五人明确分工

最终采用：

```text
A 组：数据与生物信息（P1、P2）
B 组：Baseline 与统一评价（P3）
C 组：深度生成模型（P4、P5）
```

Baseline 不再额外承担一个深度模型。它与统一评价合并，因为两项工作前后相连，而且能够平衡五个人的工作量。

### A 组：数据与生物信息（P1、P2）

完整流水线：

```text
原始数据
→ 工程清洗
→ 蓝藻 taxonomy / GvpA 生物学核验
→ 去重和聚类
→ cluster-aware split
→ 正式 MSA 和 EDA
→ dataset_v1 冻结
```

#### P1：数据工程、划分与版本负责人

P1 负责“数据怎样稳定、可重建地变成模型输入”。

工作范围：

- 建立 raw/interim/processed 目录，原始文件只读保留；
- 编写 FASTA 解析和 metadata 合并脚本；
- 处理空序列、非法字符、大小写、重复 ID、partial/fragment 等格式问题；
- 接收 P2 的生物学纳入/排除决定，并由脚本执行；
- 完成 exact dedup；
- 运行相似度聚类，并统计不同阈值的数据量；
- 以 cluster 为单位划分 train/valid/test；
- 固定 seed，输出 split_info 和 checksum；
- 完成数据规模、长度、氨基酸组成和相似度 EDA；
- 发布并维护 `dataset_v1`；
- 主笔 `dataset_report.md` 的数据流程、统计和划分部分。

P1 必须交付：

```text
preprocessing/ 数据处理脚本
metadata.csv
removal_log.csv
cluster_info.csv
train.fasta / valid.fasta / test.fasta
split_info.csv
dataset checksum
长度、组成、相似度 EDA
```

P1 完成判定：

- 从 raw 开始一条命令能够重建 processed 数据；
- 每条删除记录都有原因；
- 同一 cluster 不跨 train/valid/test；
- 数据版本和随机种子明确；
- P2 确认所有生物学筛选决定已正确落到最终文件。

P1 不负责：独自判断模糊序列是不是 GvpA，也不负责 LSTM/VAE 训练。

#### P2：蓝藻/GvpA 核验、MSA 与 family 负责人

P2 负责“留下的数据在生物学定义上是否属于目标集合”。

工作范围：

- 核验每条记录是否属于 Cyanobacteria；
- 核验 header、数据库注释和 accession；
- 区分 GvpA 与 GvpJ/GvpM/GvpK 等 GvpA-like 同源蛋白；
- 确认 family/profile 方案、数据库版本和阈值；
- 生成逐序列 `include/exclude/review` 决定及理由；
- 抽查 P1 清洗后的数据是否正确执行筛选决定；
- 使用真实比对工具构建 MSA；
- 计算 conservation、entropy、occupancy 和 gap rate；
- 对真实 train/test 运行 family/profile sanity check；
- 数据冻结后继续负责所有生成序列的 family/profile 评价；
- 进度允许时支援 ESM-2 embedding 和离群样本核验；
- 主笔 `dataset_report.md` 的数据身份、MSA 和 family 部分。

P2 必须交付：

```text
taxonomy_and_identity_review.csv
profile_config.yaml
family_scan_real.csv
msa.fasta
conservation.csv
保守性图
生物学筛选说明
```

P2 完成判定：

- 每条进入 `dataset_v1` 的序列都有蓝藻和 GvpA 证据；
- 模糊记录被单独标记，没有强行混入；
- MSA 不是简单截断得到；
- profile ID、版本、阈值、E-value 和 bit score 有记录；
- P1 能直接根据 review 表重建最终数据。

P2 不负责：维护通用 FASTA 清洗代码，也不负责为深度模型调训练参数。

#### A 组共同责任

P1 对工程正确性签字，P2 对生物学纳入标准签字。只有两人都确认后才能发布 `dataset_v1`。

```text
data/processed/dataset_v1/
├── train.fasta
├── valid.fasta
├── test.fasta
├── metadata.csv
├── removal_log.csv
├── cluster_info.csv
├── split_info.csv
├── msa.fasta
└── dataset_report.md
```

### B 组：Baseline 与统一评价（P3）

#### P3：Baseline、Evaluator 与集成负责人

P3 负责“建立最低比较标准，并让所有模型在同一规则下被评价”。

工作范围：

- 实现 AA-frequency/random baseline；
- 实现 k-mer/Markov baseline，至少支持若干 k 值；
- 从 train 数据估计长度和氨基酸/k-mer 分布；
- 冻结 generated FASTA header、run ID 和 `metrics.json` schema；
- 实现字符合法率、长度合理率和 exact copy rate；
- 实现基于明确比对规则的 nearest-train identity，并同时保存 alignment coverage；
- 实现 novelty 阈值统计；
- 实现 unique ratio、pairwise diversity 和最大簇占比；
- 实现 AA composition distance；
- 接入 P2 提供的 family/profile 扫描结果；
- 接入可选的 ESM-2 embedding/MMD 结果；
- 汇总所有模型的 `model_comparison.csv`；
- 生成模型对比图和权衡图；
- 维护根目录 README 的统一生成/评价命令；
- 组织 9 月 18 日的干净环境复现。

P3 必须交付：

```text
baseline/aa_frequency.py
baseline/kmer.py
evaluation/evaluate_all.py
baseline generated FASTA
每个 run 的 metrics.json
per_sequence_metrics.csv
model_comparison.csv
模型对比图和权衡图
根目录 README
```

P3 完成判定：

- baseline 可设置 seed 并重复生成；
- LSTM/VAE FASTA 无需手工修改即可进入 evaluator；
- novelty 来自真实序列比较，不是启发式或随机分数；
- 外部工具失败时输出 `null/error`，不填充 mock 值；
- 同一指标对所有模型使用同一阈值和实现；
- 一条命令能生成完整比较表。

P3 不负责：修改 LSTM/VAE 内部结构，也不独自解释 family/profile 的生物学含义。

### C 组：深度生成模型（P4、P5）

两人共用 tokenizer、dataset loader、训练日志和生成输出规范，但分别对一个模型的最终可用性负责。

#### P4：共享训练基础设施与 LSTM 负责人

工作范围：

- 主责统一 tokenizer；
- 主责 train/valid/test dataset loader 和 collate；
- 明确 PAD/BOS/EOS/UNK，禁止模型内写死 token ID；
- 实现 LSTM 自回归模型；
- 实现 train/validation loss、checkpoint save/load；
- 实现 temperature、可选 top-k/top-p 生成；
- 生成规定数量的 FASTA；
- 完成 temperature 权衡实验；
- 编写 LSTM 配置和运行说明；
- 为 P5 提供并维护共享 loader 接口。

P4 必须交付：

```text
configs/tokenizer.json
src/data/dataset.py
src/models/lstm.py
experiments/train_lstm.py
experiments/generate_lstm.py
LSTM checkpoint/config/log/generated FASTA
temperature 实验结果
```

P4 完成判定：

- tokenizer 和 loader 单元测试通过；
- loss 正常下降并记录 validation loss；
- checkpoint 重载后能够生成；
- 生成从 BOS 开始并在 EOS/最大长度停止；
- 输出可被 P3 evaluator 直接读取；
- 至少完成规定的 temperature 配置。

P4 不负责：替 P5 调试 VAE latent/KL 问题，也不修改评价指标定义。

#### P5：VAE 与 latent 实验负责人

工作范围：

- 复用并 review P4 的 tokenizer 和 dataset loader；
- 实现 VAE encoder、mu/logvar、reparameterization 和 decoder；
- 正确实现 reconstruction loss 与 KL loss；
- 分别记录 reconstruction、KL 和 total loss；
- 实现 checkpoint save/load；
- 实现随机 latent 采样和批量生成；
- 检查 posterior collapse 和 mode collapse；
- 完成 beta 或 latent sampling scale 权衡实验；
- 生成规定数量的 FASTA；
- 编写 VAE 配置和运行说明。

P5 必须交付：

```text
src/models/vae.py
experiments/train_vae.py
experiments/generate_vae.py
VAE checkpoint/config/log/generated FASTA
reconstruction/KL 曲线
beta 或 latent sampling 实验结果
```

P5 完成判定：

- 可以重构真实序列；
- reconstruction 与 KL 单独记录；
- checkpoint 重载后能够生成；
- 不同 latent 不会全部生成相同序列；
- PAD/BOS/EOS 与共享 tokenizer 一致；
- 输出可被 P3 evaluator 直接读取。

P5 不负责：另建一套 tokenizer/loader，也不修改评价指标使其偏向 VAE。

#### C 组共同责任

- P4 是共享 tokenizer/loader 的维护者，P5 必须 review；
- 两个模型使用完全相同的 `dataset_v1`；
- 两个模型共同确认 generate 输出 schema；
- 任一共享接口修改必须同时通知 P3；
- P4/P5 各自负责自己模型从训练到生成的完整闭环，不能只交模型类。

## 2.1 工作量转移规则

为了避免前后半程工作不均，任务按阶段转移：

| 时间点                | 人员转移 | 后续任务                                                        |
| --------------------- | -------- | --------------------------------------------------------------- |
| `dataset_v1` 冻结后 | P1       | 支援实验数据核对、真实/生成分布图、报告的数据与实验部分         |
| `dataset_v1` 冻结后 | P2       | 负责生成序列 family/profile 批量扫描，支援 ESM-2 与异常样本分析 |
| baseline 完成后       | P3       | 全力完成 evaluator、汇总表、图和全流程复现                      |
| LSTM/VAE 首次生成后   | P4/P5    | 除调参外，配合 P3 修复接口并解释模型结果                        |

没有完成本人硬交付前，不以“帮助其他组”为理由跳过自己的责任。

## 3. 两周时间表

| 时间       | 阶段       | 主要工作                                                                                                  | 到期交付                                              |
| ---------- | ---------- | --------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- |
| 9/6        | 启动       | 建仓库；确定三组人员；冻结 FASTA、metadata、tokenizer、config、结果格式；确认 GAN 与 family 判定要求      | README v0.1、目录、schema、分工表                     |
| 9/7–9/9   | 数据与骨架 | A：清洗、taxonomy/GvpA 核验、聚类；B/P3：baseline 和最小 evaluator；C/P4–P5：共享 loader、LSTM、VAE 骨架 | 数据脚本、tokenizer、模型 smoke test、evaluation v0.1 |
| 9/10–9/11 | 数据冻结   | cluster-aware split、真实 MSA、EDA、泄漏检查                                                              | `dataset_v1`、dataset report、EDA、checksum         |
| 9/12–9/14 | 首次贯通   | 跑 AA-frequency/k-mer；训练 LSTM/VAE；完成真实 novelty/diversity/family 评价                              | 各方法生成 FASTA、两个 checkpoint、对比表 v0          |
| 9/15–9/17 | 正式实验   | LSTM temperature、VAE beta/latent sampling；统一生成量和评价条件                                          | 正式 metrics、模型对比表、权衡图                      |
| 9/18       | 复现       | 停止加功能；从干净环境运行训练/加载、生成、评价和绘图                                                     | 复现记录、最终 README、bug 状态                       |
| 9/19       | 验收       | 检查完整流水线和研究结论                                                                                  | 两周版本、演示材料、下一阶段清单                      |

### 每个阶段的通过条件

#### 9 月 9 日

- [ ] raw → cleaned → clustered 可由脚本运行；
- [ ] tokenizer 与结果 schema 已冻结；
- [ ] baseline/LSTM/VAE 均完成最小运行测试；
- [ ] evaluator 能处理手工测试 FASTA。

#### 9 月 11 日

- [ ] `dataset_v1` 已冻结；
- [ ] train/valid/test 按 cluster 划分；
- [ ] 跨集合泄漏已检查；
- [ ] MSA 来自真实比对工具；
- [ ] 核心 EDA 与数据报告完成。

#### 9 月 14 日

- [ ] baseline、LSTM、VAE 均能输出统一 FASTA；
- [ ] LSTM/VAE checkpoint 可重新加载；
- [ ] evaluator 可自动处理 AA-frequency、k-mer、LSTM 和 VAE；
- [ ] 第一版模型对比表已经生成。

#### 9 月 17 日

- [ ] 正式配置、seed、日志和 run ID 完整；
- [ ] 各方法生成数量相同；
- [ ] 至少一组权衡实验完成；
- [ ] 所有正式指标来自真实计算，不使用 mock 分数。

#### 9 月 19 日

- [ ] 数据、生成、评价主线可以完整复现；
- [ ] 有模型对比表、权衡图和失败案例；
- [ ] 能明确回答哪个模型更保守、更新颖、更多样或更均衡；
- [ ] README 能让非作者运行主要流程；
- [ ] 没有把计算评价夸大为真实生物功能。

## 4. 协作方式

### 只做两次主要交接

```text
A → B/C：dataset_v1 + dataset_report.md
C → B：LSTM/VAE generated FASTA + config + checkpoint + log
```

### 每项工作都有主责和复现人

- 主责人组织工作包、合并代码并确认交付；
- P1/P2 互相复现数据流水线，P4/P5 互相复现模型入口；
- P3 主责统一评价，数据冻结后 P2 复现 family/ESM 部分、P1 复现基础指标和汇总命令；
- 不要求所有人同时学习全部生物信息和模型工具；
- 不允许关键脚本只在某个人电脑上运行。

### 第一天冻结接口

FASTA header、metadata 字段、tokenizer、config、生成输出、evaluator 字段和 run ID 在第一天确定。后续修改必须通知全组并记录原因。

### 每日同步不超过 15 分钟

每组只回答：

```text
今天交付了什么？
当前卡在哪个输入或输出？
是否需要修改冻结接口？
明天交付什么？
```

## 5. 最终主线

```text
真实蓝藻 GvpA 有什么规律？
→ 不同生成方法学到了多少？
→ 生成结果是否满足 family 与分布约束？
→ 是否避免复制训练集并保持多样性？
→ 三个目标之间存在怎样的权衡？
```

只要最终能用可复现实验回答这条主线，项目就是完整的。

---

# 第二部分：技术附录

正文用于安排工作；本附录用于实现时查阅。附录中的推荐参数不是强制要求，实际选择必须根据 `dataset_v1` 的统计结果调整。

## 附录 A：术语与项目边界

| 术语              | 本项目中的含义                     | 计算机类比                     |
| ----------------- | ---------------------------------- | ------------------------------ |
| GV / Gas Vesicle  | 微生物产生的蛋白质气囊结构         | 多组件组装出的最终结构         |
| GvpA              | 气囊的主要壳体蛋白，本项目目标序列 | 目标 sequence class            |
| FASTA             | 保存蛋白质序列的文本格式           | 带 ID 的字符串集合             |
| MSA               | 多序列比对                         | 对齐后的字符串矩阵             |
| 保守位点          | 多条真实序列在某位置偏好相同残基   | 低熵 token position            |
| Pfam/profile      | 蛋白家族的统计模型                 | family pattern matcher         |
| HMMER             | 用 profile HMM 搜索序列            | 模式匹配工具                   |
| CD-HIT/MMseqs2    | 按相似度聚类和去冗余               | near-duplicate clustering      |
| Sequence identity | 对齐后相同残基比例                 | 字符串相似度                   |
| Novelty           | 与训练集中最近序列仍有差异         | `1 - nearest_train_identity` |
| Diversity         | 生成批次内部不趋同                 | batch 内 pairwise distance     |
| ESM-2             | 蛋白质语言模型                     | `sequence -> embedding`      |
| MMD               | 两组样本分布差异                   | distribution distance          |

允许的结论：

- 生成序列在计算指标上满足 GvpA-like 特征；
- 某种生成方法在约束、新颖性和多样性之间更均衡；
- 某些候选值得进一步进行结构或湿实验验证。

不允许仅凭本项目声称：

- 序列一定正确折叠；
- 序列一定能正常表达；
- 序列一定能组装气囊；
- 序列一定具有实际生物学功能。

## 附录 B：数据规范

### B.1 数据来源优先级

1. 课程提供的 GV 资源；
2. NCBI RefSeq/Protein；
3. UniProt；
4. 其他公开数据库，仅作补充。

### B.2 metadata 最低字段

```text
sequence_id
sequence
organism
taxonomy_id
is_cyanobacteria
source_database
source_accession
length
raw_annotation
gvpa_evidence
family_profile_id
family_evalue
family_bitscore
clean_status
remove_reason
cluster_id
split
dataset_version
```

### B.3 清洗顺序

```text
保留 raw 原始文件
→ 解析 FASTA 与 metadata
→ 删除空序列
→ 统一大写
→ 检查非法字符
→ 标记 partial / fragment
→ 核验蓝藻 taxonomy
→ 核验 GvpA 注释与 family
→ exact dedup
→ similarity clustering
→ cluster-aware split
```

每个删除样本必须记录 `remove_reason`，不得直接覆盖原始数据。

### B.4 蓝藻与 GvpA 身份核验

现有课程资源可能混有非蓝藻 GvpA-like 序列，因此不能只根据文件名 `GvpA.fasta` 判定样本身份。

推荐使用多重证据：

```text
taxonomy 属于 Cyanobacteria
+ header/数据库注释支持 GvpA
+ GvpA-like family/profile 命中
+ 长度和序列特征不过度异常
```

任何只有 family 命中、但具体标注为 GvpJ/GvpM/GvpK 或其他同源蛋白的序列，不应直接作为 GvpA 正样本。

### B.5 Pfam/profile 编号说明

课程文档中的 `PF01132` 与当前数据库注释不一致：该编号对应 elongation factor P 的 OB domain，不应作为 GvpA validator。

GvpA-like family 常见的 Pfam accession 为：

```text
PF00741 / Gas_vesicle
```

但 PF00741 也可能覆盖其他 GvpA-like 蛋白，因此正式判定应结合：

- GvpA-specific annotation；
- InterPro/NCBIFam/PROSITE 等辅助证据；
- taxonomy；
- 必要时的基因邻域或人工核验。

最终采用的 profile、数据库版本和阈值必须写入配置，不得在代码中写死未经确认的 `PF01132`。

### B.6 去重和聚类

至少记录：

```text
原始条数
清洗后条数
exact dedup 后条数
95% identity 聚类后条数
90% identity 聚类后条数
```

最终阈值根据以下因素选择：

- 聚类后数据量；
- 高相似样本泄漏风险；
- 模型训练稳定性；
- 真实数据的天然保守程度。

不要先分别随机划分再去重。应先聚类，再以 cluster 为单位分配到 train/valid/test。

### B.7 数据划分

初始比例可使用：

```text
train 70%
valid 15%
test 15%
```

但优先保证：

- 同一高相似 cluster 不跨 split；
- 固定 random seed；
- 保存每条序列的 cluster 和 split；
- 用脚本验证跨集合最大 identity；
- 生成模型只使用 train 训练和估计生成分布。

### B.8 MSA

MSA 必须使用 MAFFT、MUSCLE、Clustal Omega 或其他真实比对方法。

以下方法不能作为正式 MSA：

```text
把所有序列直接截断到最短长度
```

正式 MSA 需要保留插入和缺失信息，并记录：

- 工具名称与版本；
- 命令；
- 输入数据版本；
- 序列数；
- 对齐列数；
- gap 比例。

### B.9 数据验收

- [ ] 原始数据可追溯；
- [ ] 蓝藻 taxonomy 已核验；
- [ ] GvpA 身份有证据；
- [ ] 空序列和非法字符已处理；
- [ ] partial/fragment 有明确规则；
- [ ] 删除原因完整；
- [ ] exact dedup 完成；
- [ ] similarity clustering 完成；
- [ ] cluster-aware split 完成；
- [ ] 跨集合泄漏已检查；
- [ ] 正式 MSA 已构建；
- [ ] `dataset_v1` 有版本和 checksum。

## 附录 C：EDA 规范

EDA 的目标是为模型参数和评价阈值提供依据，而不是单纯画图。

### C.1 数据规模

至少报告：

- raw、cleaned、deduplicated 数量；
- 蓝藻物种数；
- 数据来源分布；
- train/valid/test 数量；
- cluster 数量和 cluster size 分布。

### C.2 长度分布

至少报告：

- min、max、mean、median、std；
- P1、P5、P95、P99；
- histogram；
- boxplot。

用于确定：

- `max_seq_len`；
- padding；
- baseline 和深度模型的长度采样策略；
- 长度合理区间。

### C.3 氨基酸组成

统计 20 种标准氨基酸频率，输出：

- 全数据频率；
- train/valid/test 对比；
- 后续 real/generated 对比基线。

### C.4 保守性

```text
真实 MSA
→ 每列残基分布
→ Shannon entropy / conservation
→ occupancy 与 gap rate
→ 保守区域图
```

不要把不同长度序列未经比对的同一索引直接解释为同源位置。

### C.5 相似度

至少输出：

- 随机抽样或全量 pairwise identity 分布；
- 聚类前后对比；
- cluster size 分布；
- train/valid/test 之间最近邻 identity。

### C.6 EDA 最终必须形成的文字结论

```text
数据量是否足够？
长度策略是什么？
max_seq_len 设多少？
数据是否高度冗余？
哪些区域最保守？
生成时是否需要长度或保守位点约束？
测试集是否足够独立？
```

## 附录 D：统一 tokenizer 和数据加载

### D.1 推荐词表

```text
20 个标准氨基酸
+ <PAD>
+ <BOS>
+ <EOS>
+ <UNK>
```

不建议把 gap `-` 当成生成模型可以正常生成的氨基酸。MSA gap 与原始蛋白质 token 应分开处理。

### D.2 Token ID 约束

token ID 只在一个文件中定义：

```text
configs/tokenizer.json
```

所有模型和 loss 必须从同一个 tokenizer 读取：

- `pad_token_id`；
- `bos_token_id`；
- `eos_token_id`；
- `unk_token_id`；
- `vocab_size`。

禁止在不同模型中手写 `21/22/23`，以避免 PAD、BOS、EOS 混淆。

### D.3 Dataset loader 验收

- [ ] 空序列不会进入训练；
- [ ] 非标准字符按规则删除或映射为 UNK；
- [ ] BOS/EOS 正确加入；
- [ ] padding mask 正确；
- [ ] loss 忽略 PAD，而不是忽略 EOS；
- [ ] decode 在 EOS 停止；
- [ ] train/valid/test 分别加载；
- [ ] metadata 与 sequence_id 可对应；
- [ ] 物种条件只能来自真实 metadata，不能从 accession 猜测。

## 附录 E：Baseline

### E.1 AA-frequency baseline

```text
从 train 统计 AA 频率
→ 从 train 长度分布采样长度
→ 独立采样氨基酸
→ 输出 FASTA
```

这是最弱 baseline，用来判断深度模型是否学到超过一阶组成统计的信息。

### E.2 k-mer/Markov baseline

建议：

```text
k = 2 / 3 / 4
```

优先实现：

```text
前 k-1 个 token → 下一个 token 的条件分布
```

而不是把互不重叠的 k-mer 生硬拼接。

### E.3 Baseline 输出

```text
results/generated_sequences/baseline/<run_id>.fasta
results/configs/<run_id>.yaml
results/logs/<run_id>.log
results/metrics/<run_id>.json
```

记录：

- seed；
- k；
- 生成数量；
- 长度策略；
- 使用的数据版本。

## 附录 F：LSTM

### F.1 训练目标

```text
输入：  <BOS> M A V E ...
目标：  M A V E ... <EOS>
```

使用 next-token cross entropy，并忽略 PAD。

### F.2 推荐起点

```text
embedding_dim = 64 或 128
hidden_dim = 128 或 256
num_layers = 1 或 2
dropout = 0.1～0.3
```

数据量较小时不要优先扩大模型。

### F.3 生成

至少支持：

```text
temperature = 0.7 / 1.0 / 1.3
```

可选：

- top-k；
- top-p；
- min/max length；
- repetition control。

### F.4 验收

- [ ] train loss 正常下降；
- [ ] validation loss 有记录；
- [ ] checkpoint 可保存和加载；
- [ ] 从 BOS 开始，到 EOS 结束；
- [ ] 可设置 seed；
- [ ] 可批量生成；
- [ ] 输出统一 FASTA；
- [ ] evaluator 可直接读取。

## 附录 G：VAE

### G.1 基本结构

```text
sequence
→ encoder
→ mu, logvar
→ reparameterization
→ latent z
→ autoregressive decoder
→ sequence
```

### G.2 Loss

```text
loss = reconstruction_loss + beta * KL_loss
```

必须分别记录：

- reconstruction loss；
- KL loss；
- total loss；
- beta；
- latent_dim。

### G.3 推荐起点

```text
latent_dim = 32 或 64
hidden_dim = 128 或 256
beta = 0.1 / 0.5 / 1.0 中选择少量配置
```

### G.4 风险

主要风险是 posterior collapse：KL 很快接近 0，decoder 忽略 latent z。

检查方式：

- 记录 KL 曲线；
- 比较不同 z 是否生成不同序列；
- 测试 latent interpolation；
- 查看 unique ratio 和最大簇占比；
- 必要时使用 KL annealing 或降低 decoder 容量。

### G.5 验收

- [ ] 可以重构输入序列；
- [ ] reconstruction/KL 分开记录；
- [ ] checkpoint 可保存和加载；
- [ ] 能从随机 z 生成；
- [ ] 不同 z 不全部生成相同序列；
- [ ] 输出统一 FASTA；
- [ ] evaluator 可直接读取。

## 附录 H：统一评价

### H.1 评价输入

```text
generated_fasta
train_fasta
test_fasta
dataset_version
evaluation_config
```

新颖性只和 train reference 比较；真实分布分析可同时展示 train/test，但不能把 test 用于模型调参。

### H.2 字符合法率

```text
valid_char_rate = 只含 20 种标准氨基酸的序列数 / 总生成数
```

### H.3 长度合理率

推荐根据 train 定义固定区间，例如：

```text
[P1(train_length), P99(train_length)]
```

规则必须在看到生成结果之前确定，并写入配置。

### H.4 Family/profile 通过率

```text
family_pass_rate = profile 检查通过数 / 总生成数
```

必须保存：

- profile accession；
- profile/database 版本；
- HMMER 版本；
- E-value；
- bit score；
- threshold；
- pass/fail。

该指标表示“与 family profile 相符”，不等价于真实功能有效。

### H.5 Exact copy rate

```text
exact_copy_rate = 与 train 中某条序列完全相同的生成数 / 总生成数
```

### H.6 新颖性

对每条生成序列 `g_i`：

```text
nearest_identity_i = max_j identity(g_i, train_j)
novelty_i = 1 - nearest_identity_i
```

同时报告：

- mean/median nearest identity；
- P5/P95；
- identity < 70%、80%、90% 的比例；
- exact copy rate。

identity 必须基于明确的对齐或序列搜索输出。不能用“长度偏离”“组成异常”代替真实新颖性。

### H.7 多样性

至少报告：

1. unique ratio；
2. 生成批次内部平均 pairwise identity/distance；
3. 最大相似度簇占比；
4. 可选：不同 seed 的结果差异。

```text
unique_ratio = unique_sequences / generated_sequences
```

### H.8 氨基酸组成差异

比较 train 与 generated 的 20 AA 频率，可报告：

- MAE；
- Jensen-Shannon divergence；
- 频率相关系数。

### H.9 ESM-2 嵌入分布

在基础 evaluator 稳定后加入：

```text
train / test / k-mer / LSTM / VAE
→ 同一 ESM-2
→ 同一 pooling
→ embedding
→ PCA 后 UMAP/t-SNE
→ MMD 或其他分布距离
```

要求：

- ESM-2 仅推理，不微调；
- 固定模型版本和 pooling；
- 降维模型在合并后的同一批 embedding 上拟合；
- t-SNE/UMAP 只作可视化；
- MMD 作为探索性指标，不包装成生物功能证明。

### H.10 不设置未经验证的综合总分

主结果分别报告：

```text
family pass
nearest identity / novelty
diversity
AA composition distance
ESM2 distribution distance
```

可以画 trade-off 和 Pareto front，但不建议把多个启发式指标任意加权为一个 `overall score` 决定模型排名。

## 附录 I：正式实验

### I.1 实验 1：模型对比

模型：

```text
AA-frequency/random
k-mer
LSTM
VAE
```

控制变量：

- 同一 `dataset_v1`；
- 相同生成数量；
- 相同 evaluator；
- 相同阈值；
- 固定 seed；
- 最好 3 个重复。

主表：

| Model        | Family Pass ↑ | Nearest Identity | Novelty ↑ | Unique Ratio ↑ | Pairwise Diversity ↑ | AA JSD ↓ | ESM2 MMD ↓ |
| ------------ | -------------: | ---------------: | ---------: | --------------: | --------------------: | --------: | ----------: |
| AA-frequency |                |                  |            |                 |                       |           |             |
| k-mer        |                |                  |            |                 |                       |           |             |
| LSTM         |                |                  |            |                 |                       |           |             |
| VAE          |                |                  |            |                 |                       |           |             |

### I.2 实验 2：三目标权衡

LSTM：

```text
temperature = 0.7 / 1.0 / 1.3
```

VAE：

```text
两组 beta 或 latent sampling scale
```

输出：

- family pass vs novelty；
- family pass vs diversity；
- novelty vs diversity；
- 可选 Pareto front。

回答：

- 三个目标是否冲突；
- 哪些配置过于保守；
- 哪些配置过于随机；
- 是否存在相对均衡的区域。

### I.3 最小消融

不另起复杂模型，只选择一条清楚的消融：

```text
AA-frequency
→ k-mer
→ LSTM
→ LSTM + realistic length sampling
```

或者：

```text
VAE beta = 0.1 / 0.5 / 1.0
```

### I.4 ESM-2 分布分析

至少比较：

```text
real train
real test
k-mer
LSTM
VAE
```

需要同时讨论：

- 生成样本是否接近真实分布；
- 是否只覆盖很小区域；
- 是否存在明显离群点；
- 接近真实分布是否来自复制训练序列。

## 附录 J：统一工程接口

### J.1 推荐目录

```text
gvpa-sequence-generation/
├── README.md
├── requirements.txt
├── configs/
│   ├── tokenizer.json
│   ├── data.yaml
│   ├── baseline.yaml
│   ├── lstm.yaml
│   ├── vae.yaml
│   └── evaluation.yaml
├── data/
│   ├── raw/
│   ├── interim/
│   └── processed/dataset_v1/
├── preprocessing/
├── src/
│   ├── data/
│   ├── models/
│   └── utils/
├── evaluation/
├── experiments/
├── results/
│   ├── generated_sequences/
│   ├── checkpoints/
│   ├── configs/
│   ├── logs/
│   ├── metrics/
│   ├── tables/
│   └── figures/
├── docs/
└── tests/
```

### J.2 Generator 接口

命令行概念接口：

```bash
python -m experiments.generate \
  --model lstm \
  --checkpoint results/checkpoints/<run_id>.pt \
  --config results/configs/<run_id>.yaml \
  --num-samples 1000 \
  --seed 42 \
  --output results/generated_sequences/<run_id>.fasta
```

Python 概念接口：

```python
sequences = generator.generate(
    num_samples=1000,
    seed=42,
    **generation_config,
)
```

### J.3 Evaluator 接口

```bash
python -m evaluation.evaluate_all \
  --generated results/generated_sequences/<run_id>.fasta \
  --train data/processed/dataset_v1/train.fasta \
  --test data/processed/dataset_v1/test.fasta \
  --config configs/evaluation.yaml \
  --output-dir results/metrics/<run_id>
```

推荐 `metrics.json`：

```json
{
  "run_id": "lstm_20260916_h256_l2_t1.0_s42",
  "dataset_version": "dataset_v1",
  "num_requested": 1000,
  "num_generated": 1000,
  "valid_char_rate": 1.0,
  "length_valid_rate": 0.0,
  "family_pass_rate": null,
  "exact_copy_rate": 0.0,
  "mean_nearest_train_identity": 0.0,
  "novelty_lt_70": 0.0,
  "novelty_lt_80": 0.0,
  "novelty_lt_90": 0.0,
  "unique_ratio": 0.0,
  "mean_pairwise_identity": 0.0,
  "aa_js_divergence": 0.0,
  "esm2_mmd": null
}
```

字段为 `null` 表示该真实指标尚未运行，不能用 mock 数值代替。

### J.4 FASTA header

推荐：

```text
><run_id>|sample=<index>|model=<model>|seed=<seed>
SEQUENCE
```

### J.5 Run ID

```text
<model>_<date>_<short_config>_s<seed>
```

例如：

```text
lstm_20260916_h256_l2_t1.0_s42
vae_20260916_z64_beta0.1_s42
```

## 附录 K：现有材料复用原则

`gv/gv/design` 中已有模型、评分和候选展示代码，可以选择性参考，但正式项目需要重新验证。

### K.1 可以复用

- 模型类的大体结构；
- 训练循环框架；
- FASTA 读写思路；
- 部分可视化模板；
- 候选报告的版式；
- 旧论文作为背景和风险清单参考。

### K.2 不能直接继承

- 旧数据就是“蓝藻 GvpA”的假设；
- 截断得到的伪 MSA；
- 从 NCBI accession 猜测物种；
- 写死的 PAD/BOS/EOS token ID；
- 没有 train/valid/test 的训练结果；
- 缺少 checkpoint、config 和 log 的结果；
- 找不到工具时产生的 mock/随机分数；
- 未经真实搜索得到的新颖性；
- 任意加权的综合分数和基于它的候选排名。

### K.3 复用代码前检查

- [ ] 输入路径改为配置；
- [ ] tokenizer 统一；
- [ ] PAD/BOS/EOS 单元测试通过；
- [ ] 物种来自 metadata；
- [ ] MSA 来自真实比对；
- [ ] train/valid/test 分离；
- [ ] mock fallback 会显式输出 `null/error`；
- [ ] 结果绑定 dataset version、config、seed 和 commit；
- [ ] checkpoint 可重载并复现生成。

## 附录 L：算力安排

RTX 4060 8GB 足以完成主线：

- AA-frequency/k-mer：CPU；
- 数据清洗、聚类、MSA、HMMER：主要使用 CPU；
- LSTM/VAE：GPU；
- ESM-2：只做小模型 embedding inference。

建议：

- 模型从小配置开始；
- 显存不足先减 batch size；
- inference 使用 `torch.no_grad()`；
- 可使用 autocast/FP16；
- 不把 650M+ PLM 作为硬要求；
- 用共享表安排 GPU 时段；
- baseline、EDA 和 evaluator 不等待 GPU。

## 附录 M：主要风险与处理

### M.1 清洗后数据太少

- 使用更小的 LSTM/VAE；
- 把数据稀缺和记忆化作为分析结果；
- 检查阈值但不为了数量保留明显错误样本；
- 不盲目增加模型数量。

### M.2 GvpA 与其他 GvpA-like 蛋白混淆

- 不只依赖 PF00741；
- 结合数据库注释、taxonomy 和辅助 signature；
- 对模糊记录单独标记，不强行进入正样本；
- 保留人工核验清单。

### M.3 LSTM/VAE 全部生成相同序列

- 检查 train/test 泄漏；
- 调 temperature 或 latent sampling；
- 查看 KL collapse；
- 降低模型容量；
- 报告 unique ratio 和最大簇占比。

### M.4 生成结果很新但不像 GvpA

- 同时看 family/profile、长度、组成和 embedding；
- 不把 novelty 单独当成质量；
- 分析离群样本；
- 后续再考虑约束生成。

### M.5 评价工具没有安装或运行失败

- 输出失败状态和 `null`；
- 保留日志；
- 不用随机/mock 数值填充正式表格；
- 把可选重指标与基础 evaluator 解耦。

### M.6 工作包无法交接

- 使用固定目录和 schema；
- 交接前由非作者运行一次；
- 每个交付包含命令、输入、输出和示例；
- 不交接 notebook 内部状态或个人电脑绝对路径。

### M.7 实验很多但没有结论

每次正式实验结束填写：

```text
观察到了什么？
这个现象可能由什么导致？
它回答了哪个研究问题？
下一步因此做什么？
```

## 附录 N：最终交付清单

### 数据

- [ ] raw 数据；
- [ ] 清洗和删除日志；
- [ ] taxonomy/GvpA 证据；
- [ ] 去冗余和 cluster；
- [ ] train/valid/test；
- [ ] 正式 MSA；
- [ ] dataset report；
- [ ] dataset version/checksum。

### 模型

- [ ] AA-frequency/random baseline；
- [ ] k-mer baseline；
- [ ] LSTM；
- [ ] VAE；
- [ ] checkpoint；
- [ ] config；
- [ ] log；
- [ ] generated FASTA。

### 评价

- [ ] 字符合法率；
- [ ] 长度合理率；
- [ ] family/profile；
- [ ] exact copy；
- [ ] nearest-train identity；
- [ ] novelty；
- [ ] unique ratio；
- [ ] pairwise diversity；
- [ ] AA composition distance；
- [ ] ESM-2 embedding/MMD（若进入本轮交付）。

### 实验与报告

- [ ] 模型对比表；
- [ ] 权衡实验；
- [ ] 最小消融；
- [ ] 失败案例；
- [ ] 对研究问题的回答；
- [ ] 局限性；
- [ ] README；
- [ ] requirements/environment；
- [ ] 答辩材料。

---

# 文档使用说明

- 日常排期、分工和验收：只看第一部分；
- 开发某个模块前：查看对应技术附录；
- 参数有争议时：以 `dataset_v1` 的统计和实验结果为准；
- 新增任务时：先判断是否影响两周主线；
- 原完整计划继续保留作背景资料，本文件作为两周执行计划。
