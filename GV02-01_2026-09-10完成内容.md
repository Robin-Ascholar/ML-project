# GV02-01 蓝藻 GvpA 序列生成已完成内容概括

> 对照文件：`GV02-01_蓝藻GvpA序列生成_两周任务计划_2026-09-06至09-19.md`  
> 汇总日期：2026-09-10  
> 说明：本文只概括当前仓库中已经存在并经过核对或测试的内容，不把任务计划中的后续安排当作已完成事项。

## 1. 总体进度

当前项目已经从数据准备阶段推进到最小模型生成和统一评价阶段：

```text
冻结数据集
→ baseline 生成
→ evaluator 评价
→ LSTM/VAE 最小训练
→ checkpoint 加载
→ 生成 FASTA
→ 统一评价输出
```

按任务计划的阶段看，已经完成 9 月 9 日“数据与骨架”阶段的大部分内容，并提前跑通了 9 月 14 日“首次贯通”中的核心接口：baseline、LSTM、VAE 均能生成统一 FASTA，生成结果可进入 evaluator。

## 2. 数据集构建与 EDA 相关产物

这部分主要是仓库中已有的冻结数据产物，我已确认并沿用，没有修改 `dataset_v1`。

已存在并可作为后续模型输入的数据产物：

- `gvpa-sequence-generation/data/processed/dataset_v1/all.fasta`
- `gvpa-sequence-generation/data/processed/dataset_v1/train.fasta`
- `gvpa-sequence-generation/data/processed/dataset_v1/valid.fasta`
- `gvpa-sequence-generation/data/processed/dataset_v1/test.fasta`
- `gvpa-sequence-generation/data/processed/dataset_v1/metadata.csv`
- `gvpa-sequence-generation/data/processed/dataset_v1/all.mafft.fasta`
- `gvpa-sequence-generation/data/processed/dataset_v1/dataset_report.md`
- `gvpa-sequence-generation/data/processed/dataset_v1/checksums.sha256`
- `gvpa-sequence-generation/data/processed/dataset_v1/audit/`
- `gvpa-sequence-generation/data/processed/dataset_v1/profiles/`

数据状态：

- 最终冻结数据集：165 条蓝藻 GvpA；
- train/valid/test：139 / 13 / 13；
- 已有真实 MAFFT MSA；
- 已有 PF00741 和本地核心 profile 相关审计结果；
- 已有跨集合泄漏检查；
- 后续模型统一使用 `dataset_v1`，不直接修改冻结数据。

辅助预训练集也已存在：

- `gvpa-sequence-generation/data/processed/aux_pan_gvpa_v1/pretrain_95rep.fasta`
- `gvpa-sequence-generation/data/processed/aux_pan_gvpa_v1/pretrain_plus_blue_train.fasta`
- `gvpa-sequence-generation/data/processed/aux_pan_gvpa_v1/blue_finetune_train.fasta`
- `gvpa-sequence-generation/data/processed/aux_pan_gvpa_v1/dataset_report.md`
- `gvpa-sequence-generation/data/processed/aux_pan_gvpa_v1/training_recipe.md`

## 3. Baseline 已完成内容

已新增并跑通两个 baseline：

- `gvpa-sequence-generation/baseline/aa_frequency.py`
- `gvpa-sequence-generation/baseline/kmer.py`

完成内容：

- AA-frequency/random baseline；
- k-mer/Markov baseline，支持 `--k` 参数；
- 从 train 集估计长度分布；
- 从 train 集估计氨基酸或 k-mer 转移分布；
- 支持 `--seed`，可复现实验；
- 输出统一 FASTA header，包含 `run_id`、模型类型和 seed。

已生成的 baseline 结果：

- `gvpa-sequence-generation/runs/baseline/aa_frequency_seed42.fasta`
- `gvpa-sequence-generation/runs/baseline/kmer3_seed42.fasta`

baseline 评价摘要：

| run_id | 数量 | 合法字符率 | 长度范围率 | exact copy train | 平均 nearest-train identity | unique ratio |
|---|---:|---:|---:|---:|---:|---:|
| `aa_frequency_seed42` | 100 | 1.0 | 1.0 | 0.0 | 0.5583 | 1.0 |
| `kmer3_seed42` | 100 | 1.0 | 1.0 | 0.0 | 0.7295 | 1.0 |

## 4. 统一评价流水线已完成内容

已新增：

- `gvpa-sequence-generation/evaluation/evaluate_all.py`
- `gvpa-sequence-generation/evaluation/summarize_runs.py`

当前 evaluator v0.1 已实现：

- 字符合法率；
- 长度范围率；
- train exact copy rate；
- all-real exact copy rate；
- nearest-train identity；
- nearest-train query coverage；
- nearest-train target coverage；
- novelty 阈值统计；
- unique ratio；
- pairwise diversity；
- 最大重复比例；
- AA composition L1 distance；
- 每条序列的 `per_sequence_metrics.csv`；
- 每个 run 的 `metrics.json`；
- 多个 run 汇总为 `model_comparison.csv`。

已生成汇总表：

- `gvpa-sequence-generation/runs/model_comparison.csv`
- `gvpa-sequence-generation/runs/xr1_model_comparison.csv`
- `gvpa-sequence-generation/runs/xr1_model_comparison_5ep.csv`

说明：family/profile 字段当前显式输出 `null/not_run`，尚未接入 HMMER，不使用 mock 分数。

## 5. 共享训练基础设施已完成内容

已新增：

- `gvpa-sequence-generation/configs/tokenizer.json`
- `gvpa-sequence-generation/src/fasta.py`
- `gvpa-sequence-generation/src/data/dataset.py`

完成内容：

- 固定 tokenizer 版本；
- 明确 PAD/BOS/EOS/UNK；
- 固定 20 种标准氨基酸 token；
- FASTA 读取和写出；
- sequence encode/decode；
- train/valid/test Dataset；
- batch padding；
- attention mask；
- LSTM 和 VAE 共用同一套 tokenizer 与 loader。

## 6. LSTM 已完成内容

已新增：

- `gvpa-sequence-generation/src/models/lstm.py`
- `gvpa-sequence-generation/experiments/train_lstm.py`
- `gvpa-sequence-generation/experiments/generate_lstm.py`

完成内容：

- LSTM 自回归生成模型；
- train loss 和 valid loss；
- checkpoint 保存；
- checkpoint 重新加载；
- temperature 生成；
- top-k 生成；
- 从 BOS 开始生成；
- 到 EOS 或最大长度停止；
- 输出统一 FASTA；
- 生成结果可直接进入 evaluator。

已在 `xr1` 环境完成 CPU 测试：

```text
Python: 3.10.20
PyTorch: 2.11.0+cu130
CUDA available: False
Device: CPU
```

5 epoch smoke test 结果：

```text
train loss: 3.162 -> 2.475
valid loss: 3.080 -> 2.400
```

输出目录：

- `gvpa-sequence-generation/runs/xr1_lstm_5ep/checkpoint.pt`
- `gvpa-sequence-generation/runs/xr1_lstm_5ep/train_log.json`
- `gvpa-sequence-generation/runs/xr1_lstm_5ep/generated.fasta`
- `gvpa-sequence-generation/runs/xr1_lstm_5ep/eval/metrics.json`
- `gvpa-sequence-generation/runs/xr1_lstm_5ep/eval/per_sequence_metrics.csv`

## 7. VAE 已完成内容

已新增：

- `gvpa-sequence-generation/src/models/vae.py`
- `gvpa-sequence-generation/experiments/train_vae.py`
- `gvpa-sequence-generation/experiments/generate_vae.py`

完成内容：

- VAE encoder；
- `mu/logvar`；
- reparameterization；
- decoder；
- reconstruction loss；
- KL loss；
- total loss；
- checkpoint 保存；
- checkpoint 重新加载；
- random latent sampling；
- latent-scale 生成参数；
- 输出统一 FASTA；
- 生成结果可直接进入 evaluator。

5 epoch smoke test 结果：

```text
total loss:          3.074 -> 2.125
reconstruction loss: 3.074 -> 2.122
KL loss:             0.0049 -> 0.3038
```

输出目录：

- `gvpa-sequence-generation/runs/xr1_vae_5ep/checkpoint.pt`
- `gvpa-sequence-generation/runs/xr1_vae_5ep/train_log.json`
- `gvpa-sequence-generation/runs/xr1_vae_5ep/generated.fasta`
- `gvpa-sequence-generation/runs/xr1_vae_5ep/eval/metrics.json`
- `gvpa-sequence-generation/runs/xr1_vae_5ep/eval/per_sequence_metrics.csv`

## 8. 已通过的测试

已实际运行并通过：

- Python 编译检查：`python -m compileall baseline evaluation experiments src`；
- tokenizer encode/decode；
- Dataset 加载 train FASTA；
- batch padding 和 attention mask；
- AA-frequency FASTA 生成；
- k-mer FASTA 生成；
- baseline FASTA 进入 evaluator；
- LSTM 训练；
- LSTM checkpoint 保存和重新加载；
- LSTM FASTA 生成；
- LSTM 生成结果进入 evaluator；
- VAE 训练；
- VAE checkpoint 保存和重新加载；
- VAE FASTA 生成；
- VAE 生成结果进入 evaluator；
- 多个 run 汇总为 CSV；
- CPU device path。

## 9. 对照任务计划尚未完成的内容

以下内容仍不能标记为完成：

- 生成序列的 HMMER family/profile 自动评价尚未接入 evaluator；
- ESM-2 embedding、UMAP/t-SNE、MMD 尚未接入；
- LSTM temperature 正式权衡实验尚未完成；
- VAE beta 或 latent-scale 正式权衡实验尚未完成；
- 辅助预训练集的“预训练 → 蓝藻微调”对照实验尚未完成；
- 模型对比图和权衡图尚未生成；
- 当前 LSTM/VAE 只是 smoke test，不是最终正式训练配置；
- LSTM 5 epoch 生成序列容易达到最大长度，长度控制仍需改进；
- VAE 5 epoch 生成序列偏短，生成策略仍需调参；
- 本机没有 NVIDIA GPU，CUDA kernel 未在本机实测；
- `ML` 环境已创建为 Python 3.10，但 Torch 因下载过慢未完成安装；当前实际测试使用的是已有 `xr1` 环境；
- 9 月 18 日干净环境复现尚未进行。

## 10. 当前结论

当前仓库已经具备最小可运行主线：

```text
dataset_v1
→ AA-frequency / k-mer / LSTM / VAE
→ generated FASTA
→ evaluator
→ metrics.json / per_sequence_metrics.csv
→ model_comparison.csv
```

也就是说，任务计划中的数据基础、baseline、共享 loader、LSTM/VAE 最小闭环和统一评价 v0.1 已经完成并经过 CPU 测试。后续重点应放在正式实验、family/profile 评价接入、权衡实验和结果可视化上。
